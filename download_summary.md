The Kepler FITS files for the confirmed exoplanets have been downloaded.

- The `kepler_fits_files` directory now contains the downloaded FITS files, organized by KIC ID.
- The `processed_kepler_sh_downloads.json` file logs the download progress.

If you wish to download more data, ensure `data/Kepler.sh` is updated or specify a new source.